import ThemeSwitch, {
  fetcherKey,
  type Theme,
  ThemeSwitchSchema,
  updateThemeActionIntent,
} from "./ThemeSwitch";

export default ThemeSwitch;
export { fetcherKey, Theme, ThemeSwitchSchema, updateThemeActionIntent };
